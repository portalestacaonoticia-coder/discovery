# Mesmos valores do .env da raiz do radar. A service key fica SO no servidor
# (nenhuma variavel NEXT_PUBLIC_*): o painel nunca expoe o banco ao navegador.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Senha unica de acesso ao painel (Basic Auth). Vazia = painel aberto (so dev).
PAINEL_SENHA=
